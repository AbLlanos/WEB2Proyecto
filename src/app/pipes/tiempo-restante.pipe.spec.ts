import { TiempoRestantePipe } from './tiempo-restante.pipe';

describe('TiempoRestantePipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoRestantePipe();
    expect(pipe).toBeTruthy();
  });
});
