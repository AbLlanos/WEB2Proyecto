import { SuscripcionActivaPipe } from './suscripcion-activa.pipe';

describe('SuscripcionActivaPipe', () => {
  it('create an instance', () => {
    const pipe = new SuscripcionActivaPipe();
    expect(pipe).toBeTruthy();
  });
});
